#!/usr/bin/env bash

# TODO: env-opts.sh .profile differrences: pass

# {{{ utils/shell-colors.sh

export COLOR_TRUECOLOR_MODE="${COLOR_TRUECOLOR_MODE:-normal}" # true | false | normal
export COLOR_COMPAT_MODE="${COLOR_COMPAT_MODE:-auto}" # auto | mono | basic8 | basic16
export COLOR_DISABLE_LIST="${COLOR_DISABLE_LIST:-}"

# }}}
